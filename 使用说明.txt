//插件说明  .enlarge是放大的功能  .narrow是缩小的功能   .rotate是旋转功能
示例：$(".img").enlarge(0.25,3);  //参数分别表示的是 每次递增的倍数、最大倍数
示例：$(".img").narrow(0.25,0.25);//参数分别表示的是 每次递减的倍数、最大倍数
示例：$(".img").rotate(90,360);//参数分别表示的是 每次旋转的角度、最大角度
